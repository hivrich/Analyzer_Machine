# 📊 Отчёт Part Academy — Инструкция по публикации

## 🚀 Публикация на GitHub Pages

### Вариант 1: Использование папки `docs/` (рекомендуется)

1. **Включите GitHub Pages в настройках репозитория:**
   - Перейдите в Settings → Pages
   - В разделе "Source" выберите **"Deploy from a branch"**
   - Выберите ветку: **`main`** (или `master`)
   - Выберите папку: **`/docs`**
   - Нажмите **Save**

2. **URL вашего отчёта будет:**
   ```
   https://<username>.github.io/<repository-name>/reports/partacademy/
   ```
   
   Например:
   ```
   https://yourusername.github.io/analyzer-machine/reports/partacademy/
   ```

3. **Закоммитьте и запушьте изменения:**
   ```bash
   git add docs/reports/partacademy/index.html
   git commit -m "Add Part Academy investigation report (web version)"
   git push
   ```

4. **Подождите 1-2 минуты** — GitHub автоматически опубликует страницу.

---

### Вариант 2: Использование ветки `gh-pages`

Если вы хотите использовать отдельную ветку:

```bash
# Создайте ветку gh-pages
git checkout -b gh-pages

# Скопируйте index.html в корень
cp docs/reports/partacademy/index.html index.html

# Закоммитьте и запушьте
git add index.html
git commit -m "Add Part Academy report"
git push origin gh-pages
```

Затем в Settings → Pages выберите ветку `gh-pages` и папку `/ (root)`.

---

## 📝 Проверка публикации

После публикации откройте URL в браузере. Если страница не загружается:

1. Проверьте, что файл `index.html` находится в правильной папке
2. Убедитесь, что GitHub Pages включён в настройках
3. Подождите ещё 2-3 минуты (иногда требуется время на деплой)

---

## 🔗 Поделиться с командой

Просто отправьте ссылку на опубликованную страницу:

```
https://<username>.github.io/<repository-name>/reports/partacademy/
```

Страница полностью адаптивна и отлично работает на мобильных устройствах!

---

## 🎨 Особенности веб-версии

- ✅ Современный дизайн с градиентами
- ✅ Адаптивная вёрстка (работает на всех устройствах)
- ✅ Навигация по разделам
- ✅ Красивые таблицы с подсветкой
- ✅ Кнопка "Наверх" для удобства
- ✅ Поддержка эмодзи
- ✅ Готово к печати

---

## 📄 Исходный файл

Исходный Markdown-отчёт находится здесь:
```
reports/partacademy/investigation_organic_conversions_dec2024_vs_dec2025_FINAL.md
```



