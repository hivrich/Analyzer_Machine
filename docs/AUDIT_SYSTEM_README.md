# Система аудита Analyzer Machine

**Версия:** 1.0  
**Дата:** 27 декабря 2025  
**Статус:** Production Ready

---

## 🎯 Зачем нужна система аудита?

### Проблема

В процессе работы AI-агент может допускать **критические ошибки**:
- Выдумывать цифры без проверки источников
- Делать выводы на основе неверных расчетов
- Игнорировать альтернативные объяснения
- Строить прогнозы на непроверенных данных

**Пример из практики (trisystems.ru):**
> Агент утверждал: "Recommendation: +46 заявок (NEW!)"  
> Реальность: Recommendation: 0 заявок (tracking не работает)  
> Последствия: Нереалистичные прогнозы, потеря доверия

### Решение

**Слой аудита между сбором данных и формированием выводов.**

Каждая цифра, расчет, гипотеза проходят строгую проверку **ДО** попадания в отчет.

---

## 📚 Документация

Система аудита состоит из:

1. **`app/audit.py`** — код проверки данных, расчетов, гипотез
2. **`docs/AUDIT_RULES.md`** — правила строгой проверки
3. **`docs/AUDIT_CHECKLISTS.md`** — чеклисты для разных типов анализа
4. **`docs/AGENT_LOOP.md`** — обновленный процесс с фазами аудита

---

## 🔧 Как использовать

### Метод 1: CLI команды (рекомендуется)

```bash
# 1. Проверка данных для клиента
python -m app.cli audit-data trisystems 2025-10-01 2025-12-25

# 2. Проверка конкретной цифры
python -m app.cli audit-metric "visits_organic=12499" \
    --source "analysis_sources_*.json" \
    --client trisystems \
    --period "Q4 2025"

# 3. Полный аудит отчета (TODO: реализовать полностью)
python -m app.cli audit-report reports/trisystems/REPORT.md --strict
```

### Метод 2: Python API

```python
from app.audit import AuditEngine, DataPoint

engine = AuditEngine("trisystems")

# Проверка источника данных
result = engine.verify_data_source(DataPoint(
    metric="visits_organic",
    value=12499,
    source_file="analysis_sources_2025-10-01_2025-12-25.json",
    period="Q4 2025",
    context={}
))

if result.status == "failed":
    print(f"⚠️ Issues: {result.issues}")
    # НЕ использовать эту цифру!

# Проверка расчета
result = engine.verify_calculation(
    result=-3577,
    operands=[16076, 12499],
    operation="delta",
    description="SEO traffic change"
)

# Проверка гипотезы
result = engine.check_hypothesis(
    hypothesis="Форма Лаборатории сломана",
    supporting_data=[...],  # минимум 3 DataPoint
    context={"type": "conversion_drop", "magnitude": -40}
)

print(f"Confidence: {result.confidence:.0%}")
print(f"Alternatives: {result.alternative_hypotheses}")
```

---

## 🚦 Процесс работы с аудитом

```
USER REQUEST
    ↓
1. PLAN (определить client, периоды, KPI)
    ↓
2. RUN (собрать данные: sources, pages, goals, GSC)
    ↓
3. ⚡ AUDIT: Data Verification
   - Проверить все workbooks
   - Проверить актуальность (<7 дней)
   - Проверить отсутствие аномалий
   ❌ FAILED → вернуться к шагу 2
   ✅ PASSED → продолжить
    ↓
4. ANALYZE (расчеты, insights)
    ↓
5. ⚡ AUDIT: Calculation Verification
   - Проверить все дельты
   - Проверить все проценты
   - Проверить contribution (сумма = 100%)
   ❌ FAILED → вернуться к шагу 4
   ✅ PASSED → продолжить
    ↓
6. HYPOTHESIZE (формулирование выводов)
    ↓
7. ⚡ AUDIT: Hypothesis Check
   - Минимум 3 альтернативы
   - Confidence >50%
   - План проверки
   ❌ LOW CONFIDENCE → доработать гипотезу
   ✅ PASSED → продолжить
    ↓
8. REPORT (написание отчета)
    ↓
9. ⚡ AUDIT: Final Report Audit
   - Все цифры имеют источники
   - Все выводы обоснованы
   - Нет противоречий
   ❌ FAILED → НЕ публиковать
   ⚠️ WARNING → публиковать с оговорками
   ✅ PASSED → продолжить
    ↓
10. PUBLISH
```

---

## 📊 Типы проверок

### 1. Data Source Verification

**Цель:** Убедиться, что каждая цифра имеет проверяемый источник.

**Проверки:**
- Файл существует?
- Метрика присутствует в файле?
- Значение совпадает?
- Данные актуальны (<7 дней)?

**Пример:**
```python
audit = engine.verify_data_source(DataPoint(
    metric="visits_organic",
    value=12499,
    source_file="analysis_sources_2025-10-01_2025-12-25.json",
    period="Q4 2025"
))
# audit.status: "passed" | "failed"
# audit.issues: [...list of problems...]
```

---

### 2. Calculation Verification

**Цель:** Проверить правильность математических операций.

**Операции:**
- sum, delta, pct_change, avg

**Пример:**
```python
audit = engine.verify_calculation(
    result=-3577,
    operands=[16076, 12499],  # p1, p2
    operation="delta",
    description="SEO traffic delta"
)
# Проверит: 12499 - 16076 = -3577 ✅
```

---

### 3. Hypothesis Validation

**Цель:** Убедиться, что гипотеза обоснована и учитывает альтернативы.

**Критерии:**
- Минимум 3 точки данных в поддержку
- Минимум 3 альтернативные гипотезы
- Confidence >50%
- План проверки

**Пример:**
```python
audit = engine.check_hypothesis(
    hypothesis="Форма Лаборатории сломана",
    supporting_data=[
        DataPoint(...),  # CR падение -40%
        DataPoint(...),  # Трафик рост +17%
        DataPoint(...),  # Падение с Q2 2025
    ],
    context={"type": "conversion_drop"}
)

# audit.alternative_hypotheses:
# - Сезонное снижение интереса
# - Изменение цен/продукта
# - Усиление конкуренции
# - Проблемы с tracking

# audit.confidence: 0.65 (65%)
```

---

### 4. Cross-Reference Checks

**Цель:** Сверить одну метрику из разных источников.

**Примеры:**
- Трафик: Метрика vs GSC
- Конверсии: разные цели
- Периоды: YoY сравнение

**Пример:**
```python
audit = engine.cross_reference(
    metric="organic_traffic",
    sources=[
        ("metrika", 12499),
        ("gsc", 12350)
    ]
)

# Проверит расхождение: (12499-12350)/12350 = 1.2% ✅
# Если >10% → warning
```

---

## 🎯 Критерии публикации отчета

| Audit Status | Confidence | Действие |
|--------------|------------|----------|
| ✅ **Passed** | >80% | Публиковать без оговорок |
| ⚠️ **Warning** | 50-80% | Публиковать с указанием ограничений |
| ❌ **Failed** | <50% | НЕ публиковать, доработать |

---

## 📋 Чеклисты

Для каждого типа анализа есть чеклист:
- **Traffic Analysis** (15 пунктов)
- **Conversion Analysis** (18 пунктов)
- **SEO Analysis** (12 пунктов)
- **Hypothesis Validation** (10 пунктов)
- **Final Report** (20 пунктов)

См. **`docs/AUDIT_CHECKLISTS.md`**

---

## 🚨 Red Flags (критические ошибки)

**НЕ публиковать отчет, если:**
- ❌ Есть цифры без источников
- ❌ Расчеты содержат ошибки
- ❌ Гипотезы без альтернатив
- ❌ Прогнозы на непроверенных данных
- ❌ Противоречия в выводах
- ❌ Confidence <30% в ключевых выводах

---

## 📈 Метрики системы аудита

**Отслеживаем (TODO: реализовать):**
- Количество проверок на отчет
- % passed / warning / failed
- Средний confidence выводов
- Время на аудит

**Целевые показатели:**
- 100% данных проверены
- >80% confidence в ключевых выводах
- 0 критических ошибок (failed)
- <5 минут на полный аудит

---

## 🛠️ Roadmap

### v1.0 (текущая) ✅
- [x] Базовые функции проверки (data, calculation, hypothesis)
- [x] CLI команды (audit-data, audit-metric)
- [x] Документация (RULES, CHECKLISTS)
- [x] Интеграция в AGENT_LOOP

### v1.1 (planned)
- [ ] Полная реализация `audit-report` (парсинг Markdown)
- [ ] Автоматическое извлечение цифр из текста отчета
- [ ] Автоматическая проверка всех утверждений
- [ ] Dashboard с результатами аудита

### v1.2 (planned)
- [ ] LLM-powered альтернативные гипотезы
- [ ] Автоматический расчет confidence
- [ ] Integration с CI/CD (блокировать публикацию failed отчетов)
- [ ] Метрики качества аудита

---

## 💡 Примеры использования

### Пример 1: Проверка отчета перед публикацией

```bash
# 1. Собрали данные
python -m app.cli analyze-sources trisystems 2024-10-01 2024-12-25 2025-10-01 2025-12-25

# 2. Проверяем данные
python -m app.cli audit-data trisystems 2025-10-01 2025-12-25
# ✅ Audit PASSED

# 3. Формируем отчет (в Cursor или вручную)

# 4. Проверяем ключевые цифры
python -m app.cli audit-metric "visits_organic=12499" \
    --source "analysis_sources_2024-10-01_2025-12-25.json" \
    --client trisystems \
    --period "Q4 2025"
# ✅ PASSED

# 5. Публикуем
git add reports/trisystems/REPORT.md
git commit -m "Add verified report for trisystems Q4 2025"
git push
```

### Пример 2: Проверка гипотезы в коде

```python
from app.audit import AuditEngine, DataPoint

engine = AuditEngine("trisystems")

# Гипотеза: "Recommendation traffic не дает конверсий"
audit = engine.check_hypothesis(
    hypothesis="Recommendation traffic не дает конверсий из-за проблем с tracking",
    supporting_data=[
        DataPoint(
            metric="recommendation_traffic",
            value=2085,
            source_file="analysis_sources_*.json",
            period="Q4 2025"
        ),
        DataPoint(
            metric="recommendation_goals",
            value=0,
            source_file="analysis_goals_by_source_*.json",
            period="Q4 2025"
        ),
        DataPoint(
            metric="recommendation_bounce_rate",
            value=0.44,  # очень низкий — трафик качественный
            source_file="metrika_pages_by_source_*.json",
            period="Q4 2025"
        )
    ],
    context={"type": "zero_conversion", "traffic_quality": "high"}
)

print(f"Hypothesis: {audit.claim}")
print(f"Confidence: {audit.confidence:.0%}")
print(f"Alternatives:")
for alt in audit.alternative_hypotheses:
    print(f"  - {alt}")

# Output:
# Hypothesis: Recommendation traffic не дает конверсий из-за проблем с tracking
# Confidence: 70%
# Alternatives:
#   - Tracking настроен неправильно (цель не срабатывает)
#   - Воронка на главной странице не работает для этого трафика
#   - Аудитория Recommendation нецелевая (смотрят, но не покупают)
```

---

## 📚 Дополнительные ресурсы

- **AUDIT_RULES.md** — детальные правила аудита
- **AUDIT_CHECKLISTS.md** — чеклисты для разных типов анализа
- **AGENT_LOOP.md** — процесс работы агента с фазами аудита
- **app/audit.py** — код системы аудита

---

## 🤝 Contribution

Система аудита находится в активной разработке. Приветствуются:
- Новые типы проверок
- Улучшение алгоритмов генерации альтернативных гипотез
- Автоматизация парсинга отчетов
- Интеграция с LLM для оценки confidence

---

**Автор:** Analyzer Machine Team  
**Дата:** 27 декабря 2025  
**Статус:** Production Ready  
**Версия:** 1.0

**Основание:** Критические ошибки в отчете trisystems.ru показали необходимость систематической проверки всех данных, выводов и гипотез.

